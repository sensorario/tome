{
  "name": "tracker-client",
  "version": "2.0.21",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm version patch --no-git-tag-version",
    "build": "vite build",
    "postbuild": "cp .htaccess dist/.htaccess",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sensorario/sg-components": "^0.0.57",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
